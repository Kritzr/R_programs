week <- c('Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday')
for (day in week) {
  print(day)
}

count <- 1
while (count <= 5) {
  print(count)
  count <- count + 1
}